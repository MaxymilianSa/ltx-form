import type { ValueOptions } from '@/@types/date-range-selector'

export const datepickerValueOptions: ValueOptions[] = ['date from-to', 'date-from', 'date-to']

export const numericInputValueOptions: ValueOptions[] = [
  'year',
  'quarter',
  'month',
  'week',
  'day',
  'hour',
  'minute',
]
